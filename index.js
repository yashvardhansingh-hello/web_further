/*
const { EventEmitter } = require('events');
const eventEmittor = new EventEmitter();
eventEmittor.on('lunch', ()=> {
    console.log('yum');
})
eventEmittor.on('cool', ()=> {
    console.log('damn');
})

eventEmittor.emit('lunch');
eventEmittor.emit('cool');
*/

const express = require('express');

